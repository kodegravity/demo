# MongoDB connection (only required if you keep the /api/contact endpoint)
MONGO_URL=mongodb://localhost:27017
DB_NAME=hexiware

# Public base URL (used by deployment platforms)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
